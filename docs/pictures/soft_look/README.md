# The softer look (D46), before and after

Each picture: the look before (top) and after (bottom), from the preview tool
(`tools/facet_preview.sh OUTDIR hills 7 0` for before, `... 0.55` for after).
Each facet is now lit partly by the smooth slope around it (55%), so neighbouring
facets differ less in light; outlines and texture projection stay faceted.
Previews, not the game: no bloom, AO, outlines or clouds.
