# Grok T2 (English proofread): appraisal

Delivered 2026-09-26. Checked against `assets/text/en.txt` and where the code uses each text. Nothing is changed yet (build hold); the kept edits wait for the go-ahead.

**Grade: fair.** Every point it raised is real, and it stayed inside the rules. But it covered only 8 of 126 texts, and two of its fixes miss.

| Key | Now | Grok's suggestion | Verdict |
|---|---|---|---|
| `access.toggle_move` | TOGGLE-TO-MOVE | TOGGLE MOVE (matching `hud.toggle_move`) | **Keep.** One name for one setting, in the menu and on screen. |
| `graphics.ssao` | SCREEN-SPACE AO | SCREEN SPACE OCCLUSION | **Adapt.** Still jargon. Say what it does: `CREVICE SHADING` (or `SOFT CREVICE SHADOWS`). |
| `graphics.bloom` | GLOW (BLOOM) | GLOW | **Keep.** |
| `slider.fov` | FIELD OF VIEW: {0} DEG | … {0} DEGREES | **Adapt:** `FIELD OF VIEW: {0}°`. Shorter than either, understood in any language, and ° is in the font's fixed Latin-1 set. |
| `toast.recording` | RECORDING PERFORMANCE FOR 30 S - PLAY AS USUAL | drop "FOR 30 S" | **Keep.** The corner countdown (`hud.recording`) already shows the time. |
| `startup.texture_problem` | 1 TEXTURE PROBLEM - SEE … | drop the "1" | **Keep.** The code already picks this text only when there's exactly one (main.cpp), and the plural text has its own count. |
| `startup.no_graphics` | sentence case | all capitals | **Drop.** It's a Windows message box in the system font, shown before the game's own font exists; capitals there read as shouting. The capitals rule is for the game's own screens. |

**To apply after the go-ahead:** five text edits in `en.txt` (no code). Then `python3 tools/check_strings.py`: keys and slots unchanged.
