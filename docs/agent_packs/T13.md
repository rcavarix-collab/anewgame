# walkgrid helper task T13, German, second pass, part 2 of 2 (S)

Everything for this task is in this one message (made 2026-09-26). It is task T13 only: ignore any other walkgrid task you may remember from earlier chats.

You're helping build **walkgrid**, a first-person game for Windows (C++17, Direct3D 11). The world is faceted: angular ground built over a grid of one-block cells, lit by a sun, moon and sky that move on their own clocks. The owner is Ryan; the lead developer is another AI (Claude), who checks and applies everything you write at its next session.

**This message is complete, and it's about one task only: the one named at the top.** You won't get any files. The task tells you the facts you need about the game, with a few lines of code where a format matters. Trust them.

**Never stop or refuse for lack of information.** Where you'd need a detail that isn't here, pick the most sensible assumption, write it in your text as `ASSUMPTION: ...`, and carry on. Claude checks every assumption against the real code, so a labelled guess is useful and an unlabelled one is harmful.

**Hard rules, never broken:**
- no network use of any kind;
- no recorded or sampled audio (all sound is synthesized);
- no brand, product or company names, and nobody else's art, music, text or characters;
- never copy another game's look;
- everything must run well on a six-year-old PC (GTX 1060 class) at 60 frames a second; heavy effects need a setting to turn them off;
- the player sees few words, and every word the player sees comes from a text file keyed by name, so the game can be translated;
- no numbers shown to the player where a feel or a band will do.
- any research uses reliable sources only (science agencies, standards bodies, peer-reviewed work, recognised references; never forums), with every source listed; it's for theory and explanation, never for copying anyone's code, art or text.

**How to answer.** Everything goes in your reply, as text. The owner copies it into one file and Claude splits it:
1. First, a short report (template below), under the line `=== FILE: REPORT.md ===`.
2. Then each file the task asks for, under its own line `=== FILE: <name the task gives> ===`, with the content right after it (code in one fenced block).
3. If you run out of room, stop at the end of a file and write `CONTINUED: say "continue"`. When told to continue, go on with the next file; never repeat or restart one.
4. End with the line `END OF DELIVERY`.

**Before you answer, check your work once against the task's "Done when" list** and fix what you find.

**Report template:**
```
# Task <ID>: <name>
## Files
<each file: name, one line on what it is>
## Done when
<each item from the task: Yes / Partly / No, and where>
## Assumptions
<every ASSUMPTION you made, one line each, or "none">
## Self-check
1. Hard rules kept: Yes/No
2. Everything I say about the game comes from the task's facts or is labelled ASSUMPTION: Yes/No
3. What I'm least sure of: ...
## Questions for the owner or Claude
```

## Your task: T13

Facts for every T task:
- The pack carries `en.txt`: one `key = text` per line; `#` starts a comment; `{0}`, `{1}` are slots filled by the game; `\n` is a line break.
- Keep every key, in the same order, and every slot exactly. Words few and plain; capitals where the language has them.
- Leave `window.title`, `title.name` and `font` as they are.
- Use the everyday words a game in that language would use; say when you're unsure.

Hand back for each T task: the language file.

Done when: the same keys as `en.txt`, in the same order, slots matching line by line, and the key count in the report.

**T13. German, second pass, part 2 of 2 (S)**
- Facts: the pack carries the second half of the keys, in English and in the first German translation. Known problems (fix them if they're in your half): `HAUTLAUTSTÄRKE` should be `HAUPTLAUTSTÄRKE`; `SPIELE WIE GEWOHNT` should be `SPIEL WIE GEWOHNT`.
- Do: review every line in your half as a native speaker would; fix it; list each change with a reason.
- Hand back: `de_part2.txt` (your half, `key = text` lines, same order) and `changes.md`.
- Done when: the same keys as the English half, in order, with slots matching.

## English (this half)

```text
slider.fov = FIELD OF VIEW: {0} DEG
slider.music_intensity = MUSIC INTENSITY: {0}%
keys.title = KEYBINDINGS
keys.hint = CLICK A ROW, THEN PRESS THE NEW INPUT
keys.row = {0}: [{1}]
keys.waiting = {0}: PRESS INPUT (ESC CANCELS)
keys.waiting_menu = {0}: PRESS INPUT (ESC = ESCAPE)
action.forward = MOVE FORWARD
action.back = MOVE BACK
action.left = MOVE LEFT
action.right = MOVE RIGHT
action.jump = JUMP
action.break = BREAK
action.place = PLACE
action.menu = PAUSE MENU
action.save = QUICK SAVE
action.load = QUICK LOAD
action.sprint = SPRINT
action.crouch = CROUCH / SLIDE
action.library = MATERIALS
input.mouse_left = MOUSE LEFT
input.mouse_right = MOUSE RIGHT
input.mouse_middle = MOUSE MIDDLE
input.unknown = ?
hud.click_to_play = CLICK TO PLAY
hud.toggle_move = TOGGLE MOVE
hud.fps = FPS: {0}
hud.recording = RECORDING PERFORMANCE {0}
library.title = MATERIALS
library.rows = ROWS {0}-{1} OF {2} (WHEEL)
library.hint = CLICK A MATERIAL TO USE IT - DRAG IT ONTO A SLOT TO KEEP IT
material.air = AIR
material.foundation = FOUNDATION
material.meadow_grass = MEADOW GRASS
material.dry_turf = DRY TURF
material.moss = MOSS
material.dirt = DIRT
material.loam = LOAM
material.clay = CLAY
material.sand = SAND
material.gravel = GRAVEL
material.stone = STONE
material.slate = SLATE
material.sandstone = SANDSTONE
material.snow = SNOW
toast.saved = GAME SAVED
toast.save_failed = SAVE FAILED
toast.autosaved = AUTOSAVED
toast.autosave_failed = AUTOSAVE FAILED
toast.loaded = GAME LOADED
toast.load_failed = LOAD FAILED
toast.load_corrupt = LOAD FAILED (DAMAGED SAVE?)
toast.empty_slot = EMPTY SLOT
toast.screenshot = SCREENSHOT SAVED: {0}
toast.screenshot_failed = SCREENSHOT FAILED
toast.report = PERF REPORT SAVED: {0}
toast.report_failed = PERF REPORT COULD NOT BE SAVED
toast.recording = RECORDING PERFORMANCE FOR 30 S - PLAY AS USUAL
startup.texture_problem = 1 TEXTURE PROBLEM - SEE ASSETS\TEXTURES\_ERRORS.TXT
startup.texture_problems = {0} TEXTURE PROBLEMS - SEE ASSETS\TEXTURES\_ERRORS.TXT
startup.effects_failed = SOME GRAPHICS EFFECTS FAILED TO LOAD - SEE SHADER_ERRORS.TXT
startup.join = {0}  /  {1}
startup.no_graphics = walkgrid can't start: it needs a graphics card with feature level 11.0 support (about 2010 or newer).
```

## de.txt, the first translation (this half)

```text
slider.fov = SICHTFELD: {0} DEG
slider.music_intensity = MUSIKINTENSITÄT: {0}%
keys.title = TASTENBELEGUNG
keys.hint = ZEILE ANKLICKEN, DANN NEUE EINGABE DRÜCKEN
keys.row = {0}: [{1}]
keys.waiting = {0}: EINGABE DRÜCKEN (ESC BRICHT AB)
keys.waiting_menu = {0}: EINGABE DRÜCKEN (ESC = ESCAPE)
action.forward = VORWÄRTS
action.back = RÜCKWÄRTS
action.left = LINKS
action.right = RECHTS
action.jump = SPRINGEN
action.break = ABBAUEN
action.place = PLATZIEREN
action.menu = PAUSEMENÜ
action.save = SCHNELLSPEICHERN
action.load = SCHNELL LADEN
action.sprint = SPRINTEN
action.crouch = DUCKEN / RUTSCHEN
action.library = MATERIALIEN
input.mouse_left = MAUS LINKS
input.mouse_right = MAUS RECHTS
input.mouse_middle = MAUS MITTE
input.unknown = ?
hud.click_to_play = KLICKEN ZUM SPIELEN
hud.toggle_move = BEWEGUNG UMSCHALTEN
hud.fps = FPS: {0}
hud.recording = LEISTUNG AUFZEICHNEN {0}
library.title = MATERIALIEN
library.rows = ZEILEN {0}-{1} VON {2} (RAD)
library.hint = MATERIAL ANKLICKEN ZUM NUTZEN - AUF SLOT ZIEHEN ZUM BEHALTEN
material.air = LUFT
material.foundation = FUNDAMENT
material.meadow_grass = WIESENGRAS
material.dry_turf = TROCKENER RASEN
material.moss = MOOS
material.dirt = ERDE
material.loam = LEHM
material.clay = TON
material.sand = SAND
material.gravel = KIES
material.stone = STEIN
material.slate = SCHIEFER
material.sandstone = SANDSTEIN
material.snow = SCHNEE
toast.saved = SPIEL GESPEICHERT
toast.save_failed = SPEICHERN FEHLGESCHLAGEN
toast.autosaved = AUTOMATISCH GESPEICHERT
toast.autosave_failed = AUTOSPEICHERN FEHLGESCHLAGEN
toast.loaded = SPIEL GELADEN
toast.load_failed = LADEN FEHLGESCHLAGEN
toast.load_corrupt = LADEN FEHLGESCHLAGEN (BESCHÄDIGTER SPEICHER?)
toast.empty_slot = LEERER SLOT
toast.screenshot = SCREENSHOT GESPEICHERT: {0}
toast.screenshot_failed = SCREENSHOT FEHLGESCHLAGEN
toast.report = LEISTUNGSBERICHT GESPEICHERT: {0}
toast.report_failed = LEISTUNGSBERICHT KONNTE NICHT GESPEICHERT WERDEN
toast.recording = LEISTUNG 30 S AUFZEICHNEN - SPIELE WIE GEWOHNT
startup.texture_problem = 1 TEXTURPROBLEM - SIEHE ASSETS\TEXTURES\_ERRORS.TXT
startup.texture_problems = {0} TEXTURPROBLEME - SIEHE ASSETS\TEXTURES\_ERRORS.TXT
startup.effects_failed = EINIGE GRAFIKEFFEKTE KONNTEN NICHT GELADEN WERDEN - SIEHE SHADER_ERRORS.TXT
startup.join = {0}  /  {1}
startup.no_graphics = walkgrid kann nicht starten: es braucht eine Grafikkarte mit Feature Level 11.0 Support (ca. 2010 oder neuer).
```

## End of task T13

Do task T13 now: the report under `=== FILE: REPORT.md ===` first, then each file under its `=== FILE:` line, and `END OF DELIVERY` at the very end.
